diff --git a/Sources/SwiftTUIRuntime/Rendering/DefaultRenderer+CompletedFrameCandidates.swift b/Sources/SwiftTUIRuntime/Rendering/DefaultRenderer+CompletedFrameCandidates.swift
index 33f061b6..450fb685 100644
--- a/Sources/SwiftTUIRuntime/Rendering/DefaultRenderer+CompletedFrameCandidates.swift
+++ b/Sources/SwiftTUIRuntime/Rendering/DefaultRenderer+CompletedFrameCandidates.swift
@@ -126,15 +126,22 @@ extension DefaultRenderer {
     semantics: SemanticSnapshot,
     workerCustomLayoutCacheUpdates: [WorkerCustomLayoutCacheUpdate]
   ) -> CommittedFrameEffects {
-    var runtimeRegistrationDiagnostics = RuntimeRegistrationDiagnostics()
-    let (commit, commitDuration) = measurePhase(clock: draft.clock) {
-      let lifecycleEvents = viewGraph.finalizeFrame(
+    // TEMPORARY PROBE (do not land): `commit_ms` split into three measured
+    // sub-phases; the sum equals the former single span. See
+    // ``CommitBreakdownProbe``.
+    let clock = draft.clock
+    let (lifecycleEvents, finalizeDuration) = measurePhase(clock: clock) {
+      viewGraph.finalizeFrame(
         rootIdentity: draft.graphRootIdentity,
         resolved: resolved,
         placed: placed
       )
-      runtimeRegistrationDiagnostics = commitFrameHeadDraftEffects(draft)
-      return commitPlanner.plan(
+    }
+    let (runtimeRegistrationDiagnostics, txnDuration) = measurePhase(clock: clock) {
+      commitFrameHeadDraftEffects(draft)
+    }
+    let (commit, planDuration) = measurePhase(clock: clock) {
+      commitPlanner.plan(
         resolved: resolved,
         placed: placed,
         semantics: semantics,
@@ -142,6 +149,14 @@ extension DefaultRenderer {
         lifecycleEvents: lifecycleEvents
       )
     }
+    let commitDuration = finalizeDuration + txnDuration + planDuration
+    if CommitBreakdownProbe.isEnabled {
+      CommitBreakdownProbe.record(
+        finalize: finalizeDuration,
+        txn: txnDuration,
+        plan: planDuration
+      )
+    }
     applyWorkerCustomLayoutCacheUpdates(workerCustomLayoutCacheUpdates)
     frameTailRenderer.pruneMeasurementCache(
       keeping: viewGraph.liveNodeIDSnapshot()
diff --git a/Sources/SwiftTUIRuntime/Rendering/FrameHeadDraftTransaction.swift b/Sources/SwiftTUIRuntime/Rendering/FrameHeadDraftTransaction.swift
index 1cfc3e00..9960c815 100644
--- a/Sources/SwiftTUIRuntime/Rendering/FrameHeadDraftTransaction.swift
+++ b/Sources/SwiftTUIRuntime/Rendering/FrameHeadDraftTransaction.swift
@@ -86,10 +86,42 @@ package final class FrameHeadTransaction {
   ///   elision path; keep both in sync until they intentionally diverge.
   package func commit() -> RuntimeRegistrationDiagnostics {
     precondition(!didCommit && !didDiscard)
+    // TEMPORARY PROBE (do not land): lap-time the four sub-commits and record
+    // the registration publication mode. See ``CommitBreakdownProbe``.
+    if CommitBreakdownProbe.isEnabled {
+      switch graphDraft.runtimeRegistrationPublication {
+      case .all:
+        CommitBreakdownProbe.recordPublication(all: true, subtrees: false, rootCount: 0)
+      case .subtrees(let roots):
+        CommitBreakdownProbe.recordPublication(
+          all: false, subtrees: true, rootCount: roots.count)
+      case .unchanged:
+        CommitBreakdownProbe.recordPublication(all: false, subtrees: false, rootCount: 0)
+      }
+    }
+    let clock = ContinuousClock()
+    var mark = clock.now
+    func lap() -> Duration {
+      let now = clock.now
+      defer { mark = now }
+      return mark.duration(to: now)
+    }
     let diagnostics = graphDraft.commitRuntimeRegistrations(from: viewGraph)
+    let graphRegistrations = lap()
     observationDraft?.commit()
+    let observation = lap()
     presentationPortalDraft.commit()
+    let portal = lap()
     animationDraft.commit()
+    let animation = lap()
+    if CommitBreakdownProbe.isEnabled {
+      CommitBreakdownProbe.recordTxn(
+        graphRegistrations: graphRegistrations,
+        observation: observation,
+        portal: portal,
+        animation: animation
+      )
+    }
     didCommit = true
     return diagnostics
   }
diff --git a/Tools/TermUIPerf/Sources/TermUIPerf/main.swift b/Tools/TermUIPerf/Sources/TermUIPerf/main.swift
index f3476f2f..54cce870 100644
--- a/Tools/TermUIPerf/Sources/TermUIPerf/main.swift
+++ b/Tools/TermUIPerf/Sources/TermUIPerf/main.swift
@@ -1,5 +1,9 @@
 import Foundation
 
+// TEMPORARY PROBE (do not land): reads the commit-phase sub-step breakdown at
+// teardown. See `CommitBreakdownProbe`.
+@_spi(Runners) import SwiftTUI
+
 #if canImport(Darwin)
   import Darwin
 #elseif canImport(Glibc)
@@ -33,6 +37,11 @@ private func run(arguments: [String]) async throws {
     for aggregate in outcome.aggregates {
       print(AggregateReducer.format(aggregate))
     }
+    // TEMPORARY PROBE (do not land): emit the commit-phase sub-step breakdown
+    // to stderr when TERMUI_PERF_COMMIT_BREAKDOWN is set.
+    if let breakdown = CommitBreakdownProbe.summaryLine() {
+      FileHandle.standardError.writeLine(breakdown)
+    }
   case .compare(let config):
     if config.gateEnabled {
       let base = try CompareCommand.loadAggregate(from: config.baseRunDirectory)
